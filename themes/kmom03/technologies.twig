{% include 'incl/header.twig' %}

<body>
    <div class="technologies-page">
        <div class="box-title">
            {{ content }}
        </div>
        
        {% for tech in meta.techs %}
            <div class="box{{ tech.class }}">
                <a href="{{ base_url }}/technology/{{ tech.class }}">{{ tech.class }}</a>
            </div>
            
        {% endfor %}
    </div>
</body>

{% include 'incl/footer.twig' %}
</html>
